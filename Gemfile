# frozen_string_literal: true

# Support the use of non-cryptic variable names e.g. $$ ==  $PROCESS_ID
require 'English'

git_source(:github) { |repo| "https://github.com/#{repo}.git" }

source 'https://rubygems.org'

# ruby '2.6.5'

gem 'minitest'
gem 'm'
